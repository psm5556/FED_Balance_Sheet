# ── Core ──────────────────────────────────────────────────────────────
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.18.0
 
# requests: tabpfn-time-series → datasets 패키지가 >=2.32.2 요구
# 이전에 ==2.31.0 으로 고정되어 있어 충돌 발생 → 버전 제약 완화
requests>=2.32.2
 
# ── Fed 대시보드 ────────────────────────────────────────────────────────
# (FRED API 호출은 requests 로 처리하므로 별도 패키지 불필요)
 
# ── TabPFN-TS 예측 ──────────────────────────────────────────────────────
# v1.x 이상: CLIENT 모드(Cloud API), 신뢰구간 포함, GPU 불필요
tabpfn-time-series>=1.0.0
